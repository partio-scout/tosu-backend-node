const path = require('path');

module.exports = {
  "config": path.resolve('./domain/config', 'config.json'),
  "models-path": path.resolve('./domain/models'),
  "migrations-path": path.resolve('./domain/migrations')
};
